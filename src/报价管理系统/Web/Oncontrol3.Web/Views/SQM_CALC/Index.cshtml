@{
    ViewBag.Title = "计算基础管理";
    Layout = "~/Views/Shared/_LayoutTM.cshtml";
}
@{
    System.Data.DataTable dt = (System.Data.DataTable)ViewBag.Data;
}
<style type="text/css">
    .widget-body {
        min-height: 50px !important;
    }
</style>
<div id="content">
    <section id="widget-grid" class="">
        <div class="row">
            <article class="col-sm-12 col-md-12 col-lg-12 sortable-grid ui-sortable">
                <div class="jarviswidget jarviswidget-sortable" data-widget-colorbutton="false" data-widget-editbutton="false" data-widget-custombutton="false">
                    <header>
                        <span class="widget-icon"> <i class="fa fa-edit"></i> </span>
                        <h2>@ViewBag.Title</h2>
                        <div style="float:right; ">
                            <button type="submit" onclick="addNew()" class="btn btn-success">新增</button>
                            <button type="submit" id="submit_search" class="btn btn-default">查询</button>
                        </div>
                    </header>
                    <div role="content">
                        <!-- end widget edit box -->
                        <div class="jarviswidget-editbox"></div>
                        <div class="widget-body">
                            <form id="form1" class="smart-form form-backgroud">
                                <div class="row">
                                    <section class="col col-3">
                                        <label class="input">
                                            <select style="width:100%" id="CALCCODE" name="CALCCODE" class="select2" placeholder="计算基础">
                                                <option value=""></option>
                                                @for (int i = 0; i < dt.Rows.Count; i++)
                                                {
                                                    <option value="@dt.Rows[i]["CALC_BASE"]">@dt.Rows[i]["DESCRIPTION"]--@dt.Rows[i]["CALC_BASE"]</option>
                                                }
                                            </select>
                                        </label>
                                    </section>
                                </div>
                            </form>
                        </div>
                        <div class="widget-body no-padding">
                            <table id="datatable_col_reorder" class="table table-striped table-bordered table-hover" style="width:100%;"></table>
                        </div>
                    </div>
                </div>
            </article>
        </div>
    </section>
</div>
@section pagespecific {
    <script type="text/javascript">
        $(document).ready(function () {
            //$("#amenufld").hide();
            //$("#logout").hide();
            //$("#amenufld").click();
            //列定义
            var columns = [{
                "title": "操作", "data": "RID", "width": "30", "orderable": false,
                render: function (data, type, row, meta) {
                    return '<button title="编辑" class="btn btn-xs btn-default" onclick=doEdit(\"' + data + '\") ><i class="fa fa-pencil"></i></button>';
                    //+ '<button title="删除" class="btn btn-xs btn-default" onclick=deleterow(\"' + data + '\") ><i class="fa fa-times"></i></button>';
                }
            },
                            { "title": "计算基础名称", "data": "CALCNAME" },
                            { "title": "计算基础代码", "data": "CALCCODE" },
                            { "title": "类型", "data": "MDMTYPE" },
                            { "title": "关联主数据", "data": "MDMKEY" },
                            { "title": "关联主数据字段", "data": "MDMFIELDNAME" }
            ];
            var table = $('#datatable_col_reorder').DataTable({
                "processing": true,
                "serverSide": true,
                "autoWidth": true,
                "order": [[2, "asc"]],//第一次加载默认第5列日期倒排
                "language": { "info": "Total : _TOTAL_", "paginate": { "first": "<<", "last": ">>", "next": ">", "previous": "<" } },//语言
                "sDom": "<'dt-toolbar'<'col-xs-12 col-sm-6'><'col-sm-6 col-xs-12 hidden-xs'>r>" + "t" +
                    "<'dt-toolbar-footer'<'col-sm-6 col-xs-12 hidden-xs'i><'col-xs-12 col-sm-6'pl>>",//整个griddom布局
                "lengthMenu": [[4, 10, 20, 50, 100], [4, 10, 20, 50, 100]],//每页显示key text
                "displayLength": 20,//默认每页显示条目数
                "ajax": {
                    "url": "@Url.Action("Lists")",//数据源controller
                    "type": "POST",
                    "data": function (d) {//查询参数
                        d.sort = d.columns[d.order[0].column].data;//排序列名转换
                        d.order = d.order[0].dir;//asc or desc
                        d.CALCCODE = $("#CALCCODE").val();
                        //d.Status = $("#Status").val();
                    }
                }, columns: columns
            });
            $('#datatable_col_reorder tbody').on('dblclick', 'tr', function () {
                var rid = $('#datatable_col_reorder').DataTable().row(this).data()['RID'];
                location.href = '/SQM_CALC/Create?id=' + rid;
            });
        });
        $("#submit_search").click(function () {
            $('#datatable_col_reorder').DataTable().search("").draw();
            return false;
        });
        function addNew() {
            document.location.href = '/SQM_CALC/Create';
            return false;
        }
        function doEdit(rid) {
            document.location.href = '/SQM_CALC/Create?id=' + rid;
        }
    </script>
}